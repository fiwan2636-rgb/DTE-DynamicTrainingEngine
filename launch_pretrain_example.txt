# Single-GPU / default run (loads config/config.yaml by default)

python pretrain.py




# Multi-GPU run (4 GPUs) with an explicit config file
# Use torchrun for distributed (DDP) training

torchrun --nproc-per-node=4 pretrain.py --config-name=config
