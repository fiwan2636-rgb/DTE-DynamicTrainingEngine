[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dte"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "hydra-core>=1.3",
  "tqdm",
  "wandb",
  "numpy",
  "einops",
  "adam-atan2-pytorch",
]
